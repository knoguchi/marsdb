[package]
name = "marsdb-crash-harness"
version = "0.0.0"
edition.workspace = true
license.workspace = true
publish = false

[dependencies]
marsdb = { path = "../marsdb" }

[dev-dependencies]
tempfile.workspace = true
